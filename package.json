{
  "name": "server",
  "version": "1.0.0",
  "description": "mocking a json file and how to fetch data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.0"
  }
}
